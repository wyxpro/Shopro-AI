# 项目协作规则 (Agent Guidelines)

## Git 变更管理规范

### 1. 触发时机
- **文件修改触发**：当达到第 1 次包含文件修改（新增、更新、删除等）的对话交互结束时；
- **用户显式指令**：当用户主动要求提交代码或同步远程时。

### 2. 执行步骤与标准
1. **暂存变更**：
   - 执行 `git add .` 将所有工作区变更暂存。
2. **生成规范提交**：
   - 执行 `git commit -m "<中文详细提交描述>"`。
   - 提交信息必须使用**简明、专业、清晰的中文**，系统概括本轮累计修改的核心模块、功能新增或缺陷修复，拒绝模糊描述。
3. **远程同步**：
   - 执行 `git push`（或确认远程仓库状态）同步到远程仓库。
4. **状态汇报**：
   - 若没有远程分支、推送受阻或需要确认，及时向用户汇报当前 Git 状态与下一步建议。
