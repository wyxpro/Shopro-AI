# Supabase Configuration
VITE_SUPABASE_URL=https://backend.appmiaoda.com/projects/supabase313589630060507136
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJhdWQiOiJhdXRoZW50aWNhdGVkIiwiZXhwIjoyMDk0MTkyNzk0LCJpc3MiOiJzdXBhYmFzZSIsInJvbGUiOiJhbm9uIiwic3ViIjoiYW5vbiJ9.3UpUbJneKoVq-1JI3dnb1ck6byGIrdBEE-ji9qLntoQ

# Optional AI Service Keys
VITE_SILICONFLOW_API_KEY=
VITE_DEEPSEEK_API_KEY=

# GLM-5.3-Flash（Sophnet）提示词增强直连兜底 —— 密钥仅存放在 .env，禁止提交
VITE_GLM_API_KEY=
VITE_GLM_BASE_URL="/glm-api/v1"
VITE_GLM_MODEL=glm-5.3-flash

# MiniMax H3 Max (Tokendance Gateway) Video V2 —— 密钥仅存放在 .env，禁止提交
VITE_MINIMAX_API_KEY=
VITE_MINIMAX_BASE_URL="/tokendance-api"

# Qwen Wan3.0 Prime (Tokendance Gateway) 视频合成 —— 密钥仅存放在 .env，禁止提交
VITE_WAN3_API_KEY=
VITE_WAN3_BASE_URL="/tokendance-api"

# Seedance 2.0 Mini (Tokendance Gateway Ark v3) —— 密钥仅存放在 .env，禁止提交
VITE_SEEDANCE_MINI_API_KEY=
VITE_SEEDANCE_MINI_BASE_URL="/tokendance-api"

# HappyHorse 1.1 (Tokendance Gateway DashScope Async) —— 密钥仅存放在 .env，禁止提交
VITE_HAPPYHORSE_API_KEY=
VITE_HAPPYHORSE_BASE_URL="/tokendance-api"

# ─── Supabase Edge Function Secrets（务必用 `supabase secrets set` 注入，禁止硬编码兜底）───
# 3.4.3 密钥治理后，以下密钥缺失时相关函数将返回 503 并记录审计日志，不再回退到明文常量：
#   GLM_API_KEY / GLM_BASE_URL / GLM_MODEL  glm-5-3-flash 主通道 + ai-assistant callLLM 首选文本模型（Sophnet 平台）
#   DEEPSEEK_API_KEY / DEEPSEEK_BASE_URL   deepseek-v4-pro、wenxin-text-generation 主通道
#   SILICONFLOW_API_KEY                    文本降级链 + siliconflow-audio(TTS/ASR)
#   VECTRUST_API_KEY / SEEDANCE_API_KEY     seedance 视频与封面生成
#   INTEGRATIONS_API_KEY                   ai-assistant 文本 LLM 回退网关 + 图片生成网关
# 例：supabase secrets set GLM_API_KEY=xxx DEEPSEEK_API_KEY=yyy SILICONFLOW_API_KEY=zzz
