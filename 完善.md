# Shopro AI 项目全面功能评估与迭代规划

> 评估对象：商家创作端（React + Supabase）、厂商运营后台（Vue 3 + MSW）、17 个 Deno Edge Functions、21 个数据库迁移、MCP/Python 代理服务
> 评估口径：所有结论均以仓库内实际代码为准；文档/README 宣称但代码未见实现的能力，明确标注「已设计未完整实现」或「仅展示/原型」。
> 状态图例：✅ 真实闭环 ｜ 🟡 部分实现 ｜ 🟠 仅展示/Demo 态 ｜ ⚪ 已设计未实现

---

## 一、项目总体判断（结论先行）

**定位**：面向抖音/TikTok 电商商家的 AIGC 带货视频 SaaS，双端架构（React 商家创作端 + Vue 厂商运营后台），后端依托 Supabase BaaS + Deno Edge Functions。

**总体结论**：这是一个**核心链路真实可用、外围功能大量演示态、后台完全原型化**的"比赛/答辩导向"高完成度原型，距离真正的商业化 SaaS 还有一次系统性的安全与架构治理。

1. **已形成真实闭环的模块（可对外交付）**：
   - 认证（Supabase Auth 邮箱 + 短信代理验证码，全站受保护路由，见 `src/App.tsx` L120-130）；
   - 「商品导入 → AI 脚本（SSE 流式）→ 积分扣除 → Seedance 视频生成 → 首帧封面持久化 → 作品库」主干工作流（`src/pages/HomePage.tsx`、`src/lib/sse.ts`、`supabase/functions/seedance`）；
   - 微信支付 Native 下单 + 回调解密入账 + 幂等防重放（`create-payment-order` / `wechat-payment-webhook`，真实使用 `wechatpay-axios-plugin`，密钥走环境变量）；
   - 数据库层：21 个迁移、主流业务表 RLS 覆盖、持久化限流 RPC（`00013_rate_limit_rpc_and_deduct_credits_v2.sql`）、原子扣积分 RPC、`error_logs`/`llm_cache` 等治理表。

2. **未闭环 / 风险最高的结构性缺陷**：
   - **计费体系双轨且可被绕过**：前端 `deductUserCredits`（`src/hooks/useCredits.ts` L77-185）是浏览器端非原子"读-改-写"，未调用数据库 RPC；`user_plans` 表 RLS 为 `FOR ALL`（`00002` L99-101），登录用户可直接改写自己的 `credits_total/credits_used`；`ai-assistant` 网关不校验 JWT、信任客户端传入的 `user_id`，且扣费/审核/RPC 失败均 fail-open 放行。
   - **异步任务无服务端推进器**：`video_jobs` 表和 Realtime publication 已建，但无任何 worker/pg_cron 消费；任务状态由用户浏览器 5s 轮询写回（`HomePage.tsx` L847-940），**关闭页面即任务悬挂在 processing**，失败退款无自动闭环。
   - **"多轨道剪辑→合成导出"未打通**：`VideoEditPage.tsx`（3475 行）编辑结果只作为 metadata 存入 `video_projects`，"导出"实际是提交一个纯 prompt 的 Seedance 任务（L2911-2951），轨道/字幕/声轨并未参与合成，时间轴混剪是可视化演示。
   - **厂商运营后台 100% MSW Mock**：`VITE_USE_MOCK=true`（`.env.example`），18 个 View + 8 组 handler 全在浏览器内模拟数据；Spring Boot 真实后端不在仓库中，与商家端数据完全未打通——属"高完成度原型"而非可部署中台。
   - **演示态功能占比大**：竞品抓取与直播高光用 LLM 生成"幻觉数据"+ `Math.random` 兜底（`phase3-assistant` L91-104、L153-156）；跨平台发布直接写 `"(模拟)"` 与假链接（L377-381）；`DataDashboardPage`、`AiToolboxPage`、`NotificationsPage`、`TemplatesPage`、`InvitePage` 等页面无任何数据源调用（静态硬编码）；`ProductSelectionPage`（3355 行）仅 1 处 Supabase 调用，16 国选品矩阵与"数据引擎已连接"基本为演示。

3. **工程质量画像**：
   - 亮点：全站路由级懒加载 + ErrorBoundary、RLS 意识、持久化限流、真实支付签名链路、SSE/音频/视频封装完整、后端有降级 Fallback 链（`deepseek-v4-pro` L80-83）。
   - 债务：4 个 1500-3500 行巨型页面组件、`routes.tsx` 与 `App.tsx` 双路由配置漂移、`credit_logs` 字段两套写法（前端 `type/credits_after` vs RPC/webhook `action/balance_after`）、零测试（`package.json` 无 test 脚本）、`HomePage` 遗留 19 处 console.log、已提交的 SiliconFlow 明文 API Key（3 个函数源码硬编码兜底值）。

---

## 二、分层分析

### 2.1 前端（商家创作端，React 18）

**路由与权限**
- ✅ 全站登录门禁：未登录仅可访问 `/landing`、`/login`（`App.tsx` L120-130），登录后全部路由包在 `MainLayout` 内；有 `ErrorBoundary`、`Suspense` 骨架（L59-102）。
- 🟠 缺陷：`src/routes.tsx` 仍保留一份只含 17 条路由的旧配置（含未在 App 使用的 `VideoCreatePage` 直挂路径），与 `App.tsx` 的 38 条路由漂移，属死代码，易误导维护者。
- ⚠️ 无角色/套餐维度的前端功能门禁：免费与付费用户可见同样 42 页（RBAC 仅后台文档提及，商家端未实现）。

**页面完成度分层（按数据源扫描 `supabase.from` / `functions.invoke` / 硬编码）**
- ✅ 真实数据页：`HomePage`(16 处 DB + 3 处函数)、`WorksPage`(11)、`VideoEditPage`(9)、`ProductsPage`、`BatchCreatePage`(9)、`ProfilePage`(7)、`ActivitiesPage`(7)、`LoginPage`(9)、`CreditsPage`(3)、`AvatarsPage`(5)、`ScriptPage`、`KnowledgePage`、`LLMCachePage`(查 llm_cache)、`TaskQueuePage`、`ABTestPage`。
- 🟡 半接入页：`CompetitorPage`/`StyleCopyPage`/`LiveHighlightPage`/`MultiLangPage`/`EmotionAnalysisPage`/`PersonalizePage` 调用了 edge function，但上游返回的是模拟/LLM 幻觉数据（见 2.3）；`AnalyticsPage` 查库但混入 `Math.random` 造数；`PublishPage`/`OpenAPIPage` 走 `phase3-assistant`（发布为模拟，见 2.2）。
- 🟠 纯展示页（0 数据源）：`DataDashboardPage`(428 行)、`AiToolboxPage`(387 行)、`NotificationsPage`(421 行)、`TemplatesPage`(280 行)、`InvitePage`(508 行，邀请码来自 profile 但未做统计链路)、`OrderDetailPage`(174 行，依赖路由传参非查库)、`ProductSelectionPage`(3355 行仅 1 处 DB 调用)、`TeamSpacePage`（team 表已建，页面 0 处 supabase 调用，疑为本地态演示，需人工复核）。

**状态管理与跨页传递**
- 仅 `AuthContext` + react-query + `window` 自定义事件（`credits_changed`）三种机制。积分同步靠事件广播（`useCredits.ts` L171），跨标签页不一致；`video_jobs`/`orders` 已启用 Supabase Realtime publication（`00012` L141-142）但前端未订阅，属"后端能力已建、前端未接"。
- 跨页选品靠 `RedirectWithState` 透传 `location.state`（`App.tsx` L7-10），刷新后丢失，靠 `useDraft` 的 localStorage 兜底，链路脆弱。

**移动端与体验**
- 响应式基建在（shadcn `sidebar.tsx` 的 `useIsMobile` 抽屉、MainLayout 846 行），但页面级适配未逐页验证；`VideoEditPage` 多轨道编辑器在移动端的可操作性存疑（大量 `hidden md:inline` 说明只做了控件降级）。

**可视化编辑器**
- 🟡 编辑体验本身实现度较高（轨道、撤销/重做、SRT 字幕导出、素材上传入库），但**导出语义与用户预期不符**：`handleExport` 仅保存 metadata + 调 `generate_video`，前端剪辑不参与真实合成，需要么接入服务端合成（ffmpeg/ ims 类），要么明确产品语义为"分镜辅助 + AI 整段生成"。

### 2.2 后端（Supabase：数据库 / Edge Functions / 计费支付 / 任务）

**数据库（21 个迁移）**
- ✅ 表覆盖面广：profiles/products/materials/video_projects/video_jobs/scripts/style_analyses/diagnostics/knowledge_entries(含 pgvector+FTS，`00007`)/competitors/llm_cache/rate_limit_windows/error_logs/orders/credit_logs/user_plans/plans/teams/team_members/team_invitations/api_keys/publish_tasks/ad_performance/trending_patterns/user_style_preferences/batch_jobs(+items)。
- ✅ RLS 普遍 `user_id = auth.uid()`；orders 仅 SELECT+INSERT 策略（`00012` L47-51），客户端无法改支付状态，设计正确。
- 🔴 高危策略：`user_plans FOR ALL`（`00002` L100）→ 任何登录用户可直接 update 自己积分，**计费体系可被前端绕过**；`video_jobs FOR ALL` 同理由客户端任意改写任务态。avatars/templates 公共读 + `INSERT WITH CHECK (true)`（`00005` L12）允许认证用户写入任意公共模板。
- 🟠 一致性债：`deduct_credits` v1 用 `credits_remaining`（`00009`），v2 改为 `credits_total-credits_used`（`00013`）；`credit_logs` 存在 `action/balance_after` 与前端 `type/credits_after/description` 两套字段写入（前端还有 catch 后删字段的补丁式回退，`useCredits.ts` L163-168），说明表结构与代码已发生漂移。

**Edge Functions（17 个）**
- ✅ 真实实现：`create-payment-order`（JWT 校验、原生微信 V3 SDK、env 配置齐全、订单入库）；`wechat-payment-webhook`（AES-GCM 解密 + pending→paid 条件更新幂等）；`seedance`/`stepaudio`/`siliconflow-audio`/`kling`/`sora`/`minimax`/`wenxin` 为真实上游代理；`deepseek-v4-pro` 有真实串行 fallback 链；`send-sms-code`/`verify-sms-code` 走 appmiaoda 短信网关。
- 🔴 认证缺失：`ai-assistant` 主体逻辑**不验证 Authorization JWT**，`userId = body.user_id` 由客户端任意声明（`index.ts` L253-255），且用 `SERVICE_ROLE_KEY` 直写库 → 可冒充他人扣积分、越过限流（限流 key 也可伪造）、以他人身份读写数据。`seedance` 等代理函数完全无鉴权，公网可得即可刷调用量烧钱。
- 🔴 密钥治理：`sk-` 开头的 SiliconFlow key 明文硬编码在 3 个已入库源码中（`deepseek-v4-pro` L81、`wenxin-text-generation` L79、`siliconflow-audio` L22）；`api/dxkp.py` L6 亦有硬编码 key；`seedance` 支持从函数目录 `key.txt` 读密钥的旁路（L38-39）。
- 🟡 扣费/风控 fail-open：`deductCredits` 在"无套餐、RPC 失败、任何异常"时均返回 allowed:true（L142-157）；`content_moderation` 异常时"默认放行"（L948）。演示友好但商用即漏洞。
- ⚪ 无服务端任务推进：无 pg_cron/pg_net/scheduler，`video_jobs` 建了 attempts/queue_position/max_attempts 字段（`00012` P2-M05）却没有消费进程——字段先行、引擎缺位。
- 🟠 CORS 一律 `*`；webhook 在密钥未配置时静默吞回调返回 200（L40-43），排障困难。

**厂商运营后台（Shopro-backend，独立评估）**
- 🟠 全部 8 个业务域（dashboard/customers/credits/workflows/risk/billing/tickets/system）由 MSW 在浏览器内提供数据，`mocks/seed` 重置演示库；接口契约（`ApiResponse<T>/PageResult<T>`、`/api/admin/**`）设计规范，但 Spring Boot 服务端**不在仓库**，后台看到的"AI 履约、Attempt 日志、风控证据链、退款"与商家端真实数据库零打通。属于"契约完整的可演示原型"，价值在于给后端团队立标准，而非可运维中台。

**MCP / Python 服务（api/、mcp/）**
- ✅ 真实可运行：4 个 FastMCP 服务（HTTP + stdio 双形态），封装脚本生成/音频/视频任务，带结构化日志与 trace_id；`api/dxkp.py` 为 ASGI 透传代理。
- ⚠️ 与主站关系未定义：MCP 工具直接打到 Vectrust 上游，绕过 Supabase 计费与 `llm_cache`，若对外开放等于开了一条不计费后门；Docker 部署文件在但无鉴权中间件说明。

### 2.3 AI 服务

**模型接入与网关**
- ✅ 真实：统一网关 `ai-assistant` 18 个 action、多模型矩阵（DeepSeek/StepAudio/CosyVoice2/Seedance/Kling/Sora/MiniMax/文心）、文本 SSE 流式（前后端均有实现）、`deepseek-v4-pro` 的降级 fallback 链。
- 🟠 与文档宣称的差距：README/商业计划书所述"100ms 健康心跳检测无缝降级"未见任何心跳/健康探测代码，fallback 是同步串行重试；"7 大视频模型矩阵"中 happyhorse/wan/Krea/Luma/pixverse 仅在工作台作为可选 label 出现，未见独立接入通道（`HomePage.tsx` 统一走 seedance 函数）；"XGBoost 完播率预测"（`docs/ai/ai.md` 架构图）**代码零实现**，`analyze_traffic` 实为 LLM 估算；"数字人口型毫秒级对齐/嘴型音素时间戳"无任何实现痕迹，情绪分析只输出 JSON 标签，不驱动渲染。
- ⚠️ 供应商集中：文本/图像/短信/封面统一经 `appmiaoda.com` 个人网关与 `draw.openai-next.com`（Vectrust）代理，非官方直连；上游封禁或域名失效将同时瘫痪多个"独立"模型通道，宣称的容灾实际同源。

**缓存与成本**
- 🟡 `llm_cache` 表与 24h TTL 真实存在，但命中计数写法是 bug（`ai-assistant` L76 把 `supabase.rpc()` builder 对象塞进 update 字段，永不生效）；cache key 用 32 位字符串 hash（L103-111）而非文档宣称的 SHA-256，存在碰撞串用响应风险。
- ⚪ 成本控制无预算/配额维度：只有请求频率限流（`rate_limit_windows`），没有 per-user token 预算、日成本上限、模型调用台账表（`error_logs` 只记错误不记用量），无法支撑商业计划书的"算力毛利"核算。

**效果评估**
- ⚪ 无 Prompt 版本管理落库（`prompt_templates` 表已建，但网关不读取启用版本做 A/B）、无生成质量回流标注、无评估集；`ABTestPage`/`DataFeedbackPage` 有页面有表，但真实投放数据无导入通道（`ad_performance` 表无写入来源），"数据驱动自进化"目前是叙事不是机制。

---

## 三、表格总结

### 3.1 已有功能盘点表

| 功能模块 | 所属层级 | 当前状态 | 主要证据 / 实现位置 | 已知问题或技术债 | 完善迭代建议 | 优先级 |
|---|---|---|---|---|---|---|
| 登录注册（邮箱+短信验证码） | 前端+Supabase Auth | ✅ | `src/contexts/AuthContext.tsx`、`LoginPage.tsx`(9 处 DB)、`send/verify-sms-code` 函数 | profile 兜底创建写在客户端；invite_code 用 Math.random 非加密随机 | profile 创建改为 auth trigger；invite_code 用 `gen_random_uuid` 派生 | P0 |
| 受保护路由/全站门禁 | 前端 | ✅ | `App.tsx` L120-130 | `routes.tsx` 为漂移死配置 | 删除 routes.tsx，路由收敛为单一配置源 | P2 |
| 商品 URL/口令导入 | 前端+AI | 🟡 | `ProductsPage.tsx`(7 处 DB)、`extract_url_selling_points` | 网页抓取依赖 LLM 网关代理，抓不到 HTML 时输出为推测内容 | 引入服务端真实抓取（jina/fetch+去噪），失败显式标注降级 | P1 |
| CoT 四层脚本 SSE 流式生成 | 前端+AI | ✅ | `src/lib/sse.ts`(441 行)、`deepseek-v4-pro`、HomePage/ScriptPage/VideoCreatePage 接入 | 流式失败无断点续传；错误处理 console 较多 | 统一 onError 重试与降级为一次性返回 | P2 |
| 积分扣除与流水 | 前端+DB | 🟡 | `useCredits.ts` L77-185、`00013` deduct_credits RPC、credit_logs | 前端读改写非原子、未调 RPC；RLS 允许用户自改 user_plans；日志字段两套并存 | 全站唯一入口改 `supabase.rpc('deduct_credits')`；user_plans 收缩为 SELECT-only；统一 credit_logs schema 并迁移 | **P0** |
| 微信支付充值闭环 | Edge+DB | ✅ | `create-payment-order`、`wechat-payment-webhook`、`query-payment-status` | 密钥未配置时 webhook 静默吞包；无对账任务 | webhook 增加签名头校验与失败告警；建每日对账 cron（订单 vs credit_logs） | P1 |
| Seedance 视频生成 | Edge+前端 | 🟡 | `seedance/index.ts`（真实上游）、HomePage 提交+5s 轮询 | 函数无鉴权；状态靠浏览器写回，关页即悬挂；无自动退款 | 提交写 video_jobs，由服务端推进（见 P1 队列项）；函数校验 JWT | **P0/P1** |
| 异步任务队列 video_jobs | DB | ⚪ 半成品 | `00004` 建表、`00012` 扩 attempts/queue_position、Realtime publication | 无消费 worker、无 pg_cron；前端未订阅 Realtime | 方案 A：Supabase Cron + worker 函数消费；方案 B：pg_net 回调；前端订阅 realtime 更新进度 | P1 |
| 多轨道可视化编辑器 | 前端 | 🟡 剪辑UI真、合成假 | `VideoEditPage.tsx`（3475 行，轨道/撤销/SRT 导出/素材入库） | "导出"仅提交 prompt 生成任务，时间轴不参与合成 | 短期：明示"分镜辅助生成"语义；中期：edge 侧 ffmpeg 按 shot 拼接/字幕烧录 | P1 |
| 作品库/首帧封面 | 前端+DB | ✅ | `WorksPage.tsx`(11 处 DB)、`lib/videoFrame.ts` | 封面截取在客户端，Safari 大视频兼容风险 | 保留，失败时回退占位图逻辑已可，加埋点 | P3 |
| 素材库分片上传 | 前端+Storage | 🟡 | MaterialsPage/VideoEditPage 上传 | 未见真正分片断点续传（README 宣称 3.4-6） | 大文件用 tus/uppy 分片或明确上限 | P2 |
| 团队协作/邀请 | Edge+DB | 🟠 | `00015` teams 三表、`phase3-assistant` invite | 无邮件送达，前端 TeamSpacePage 未见库调用；成员权限未在执行链路生效 | 接邮件服务（Resend）；资源 owner 校验落到 RLS 团队策略 | P2 |
| OpenAPI 密钥 | Edge+DB | 🟡 | `api_keys` 表（key_hash/scope/rate_limit 字段完备）、phase3 generate/revoke | 无任何网关消费 ak 验证；MCP 旁路不计费 | 在 ai-assistant 增加 `Authorization: ak_` 分支校验 + 调用计数 | P2 |
| 跨平台发布 | Edge | 🟠 模拟 | `phase3-assistant` L357-381 直接写"发布成功（模拟）"假 URL | 假数据入库易被误信 | 改为"导出物料包+人工发布打卡"真实可用流；状态标注 demo | P2 |
| 竞品监控/直播高光 | Edge+AI | 🟠 LLM 幻觉+随机兜底 | `phase3-assistant` L91-104、L153-156 | 无真实抓取/ASR；用户可付费买到假报告风险 | 高光接真实 ASR（stepaudio 已有能力）；竞品接三方数据 API 或明示演示 | P2 |
| 数据看板 Analytics/DataDashboard/ROI | 前端 | 🟠 | AnalyticsPage 混入 Math.random；DataDashboardPage 0 数据源 | 图表口径不可信 | 统一由 video_projects+ad_performance 聚合成 RPC 视图；无数据时显示空态而非造数 | P2 |
| A/B 测试与数据回流调优 | 前端+DB | 🟠 | ABTestPage(3 处 DB)、DataFeedbackPage、`generate_ab_variants` | 变体可生成，但投放数据无导入链路，"一键调优"基于模拟分 | 提供 CSV 导入 + Webhook 回流两张表；调优改为人工触发 | P3 |
| LLM 缓存与命中管理 | Edge+DB | 🟡 | llm_cache 表、ai-assistant L60-111、LLMCachePage | 命中计数 bug（L76）；32bit hash 非 SHA-256 | 改 SHA-256；`hit_count = hit_count+1` 原生 SQL 更新 | P1 |
| 内容审核 content_moderation | Edge | 🟡 | ai-assistant L945-948 | 异常默认放行；无敏感词字典前置层；结果未与后台风控联动 | fail-close + 词表初筛 + 写 risk_events 供后台审 | **P0** |
| 厂商运营后台 8 大模块 | Vue Admin | 🟠 全 MSW | `.env.example VITE_USE_MOCK=true`、`src/mocks/handlers/*` | 与真实数据零打通；Spring Boot 未落库 | 优先把 workflows/risk/orders 三域接 Supabase 真实表（字段已具备） | P2/P3 |
| MCP 工具服务 | Python | 🟡 | `mcp/mcp_server*.py`、Docker 文件 | 无鉴权、绕过计费 | 加 API Key 校验并复用 api_keys 表；调用写 credit_logs | P2 |
| XGBoost 完播率预测/数字人口型对齐/风格模型微调 | 文档 | ⚪ 已设计未实现 | `docs/ai/ai.md`、`商业计划书.md`、PersonalizePage(2 处 invoke 占位) | 文档宣称与实现差距是答辩/融资风险点 | 从 README 降级为 Roadmap，或做最小可用版 | P3 |

### 3.2 前后端与 AI 服务问题矩阵

| 模块 | 风险点 | 影响范围 | 优化方案 | 实施难度 | 建议排期 |
|---|---|---|---|---|---|
| 计费/积分 | 前端非原子扣减 + `user_plans` RLS FOR ALL 可自改 | 直接资金损失、对账失真 | RLS 改只读；扣减只走 `deduct_credits` RPC；网关统一计费，前端只展示 | 低-中 | **W1（P0）** |
| AI 网关 | `ai-assistant` 信任客户端 user_id、无 JWT 校验 | 冒用身份、越权读写、绕过限流 | 从 `Authorization` 头 `getUser()` 解析身份；拒绝 body 传入 user_id | 低 | **W1（P0）** |
| 密钥安全 | SiliconFlow/dxkp 明文 key 提交在仓库；seedance key.txt 旁路 | 密钥泄露、费用被刷 | 立即轮换全部泄露 key；统一 `Deno secrets`；仓库加 secretlint/gitleaks 钩子 | 低 | **W1（P0）** |
| 视频代理函数 | seedance/kling/sora/minimax/wenxin 无鉴权裸奔 | 上游配额与费用被公网滥用 | 统一 JWT 校验中间件（复制 create-payment-order 模式）；按用户限流 | 低 | W2（P0） |
| 异步任务 | 无服务端 worker，任务态靠浏览器写回 | 关页悬挂、无法退款、后台不可观测 | Supabase Cron + tick 函数消费 video_jobs；attempts/max_attempts 生效；终态失败自动退积分 | 中 | W3-W6（P1） |
| 剪辑导出 | 时间轴不参与合成，语义误导 | 用户预期落差、口碑风险 | 短期文案与字段诚实化；中期 edge ffmpeg 合成（先字幕+拼接） | 中-高 | W6-W10（P1） |
| 内容风控 | moderation fail-open、无词表 | 违法违规内容产出（平台封号/合规） | fail-close；违禁词库初筛 + LLM 复核；事件落库供后台审计 | 低-中 | W2-W4（P0/P1） |
| 数据可信度 | 竞品/高光/发布/看板为模拟数据入库 | 商业承诺与法律风险 | 演示数据统一打 `is_demo` 标记 + UI「演示」角标；真实功能逐项替换 | 低 | W2-W8（P1） |
| 缓存治理 | 命中计数 bug、弱 hash | 成本统计失真、缓存串用 | SHA-256 key；修复计数 SQL；LLMCachePage 展示真实命中率 | 低 | W3（P1） |
| 后台中台 | MSW 与真实库零打通 | 运营不可用、融资尽调风险 | 契约不变，将 handlers 逐域替换为 Supabase REST/RPC 适配层 | 中 | W8-W14（P2） |
| 工程化 | 零测试、巨型页面、双路由配置 | 迭代速度与回归风险 | 计费/鉴权链路补 vitest 单测；HomePage/ProductSelection/VideoEdit 按 feature 拆分；收敛路由 | 中 | 持续（P2） |
| 模型容灾 | 供应商同源（miaoda/openai-next 代理）、心跳降级仅文档 | 单点上游故障全站 AI 不可用 | 关键通道至少一一直连官方 API；fallback 记录切换日志；健康探测定时任务 | 中 | W6-W12（P2） |
| MCP 开放 | 不计费旁路 | 算力白嫖 | ak 鉴权 + 计费写库 + 与 OpenAPI 页统一密钥 | 低 | W4-W6（P2） |
| 可观测性 | error_logs 有表无面板、无指标告警 | 故障发现滞后 | 最小运维页（近 7 天错误分布/网关延迟/成本），后台 System 域承接 | 低-中 | W6-W10（P2） |

### 3.3 优先级路线图

| 阶段 | 目标 | 推荐任务 | 依赖项 | 验收标准 |
|---|---|---|---|---|
| **P0：安全与计费止血**（第 1-2 周） | 堵住资金与身份漏洞，消除已泄露密钥 | ① 轮换并迁移全部硬编码 key 至 secrets + gitleaks 钩子；② ai-assistant/seedance 等函数统一 JWT 校验；③ user_plans/video_jobs RLS 收缩为只读（写走 RPC/service_role）；④ 前端扣费统一走 `deduct_credits` RPC，credit_logs 字段归一；⑤ moderation/扣费改 fail-close | Supabase 权限迁移演练（staging 库） | 渗透自查清单通过：普通用户改积分请求被 RLS 拒绝率 100%；模拟并发 50 单扣费无超扣；仓库扫描 0 明文 key；未登录调用网关返回 401 |
| **P1：异步架构落地**（第 3-6 周） | 任务生命周期脱离浏览器、可观测可退款 | ① Supabase Cron（或外部定时）驱动 `video_jobs` 状态机：queued→processing→succeeded/failed，attempts 重试；② 失败自动调 `refund_credits` RPC 写回日志；③ 前端订阅 Realtime 更新进度（WorksPage/TaskQueuePage）；④ 生成链路服务端落 request/trace 关联 `error_logs` | P0 完成（RPC 与权限就绪） | 提交任务后关闭浏览器，任务仍在 ≤N 分钟到达终态；失败任务 100% 自动退还积分且流水可查；任务列表跨端实时刷新 |
| **P1：数据诚实性与剪辑语义**（第 4-8 周，与上并行） | Demo 态显性化，核心承诺补齐 | ① 竞品/高光/发布/看板数据统一 `is_demo` 标记 + UI「演示模式」角标与开关；② VideoEdit 导出语义修正（标注"按分镜 Prompt 整段生成"）或实现 edge ffmpeg 字幕烧录+按镜拼接最小版；③ DataDashboard/Notifications/Templates 接入真实查询或合并降级路由 | 设计确认哪些功能进本期真实范围 | 页面不再有"看似真实实为随机"的数据；导出视频至少包含字幕轨烧录结果或明确文案说明 |
| **P2：中台打通与增长闭环**（第 9-14 周） | 厂商后台从 MSW 转真实运营可用 | ① workflows/risk/orders 三域 handler 替换为 Supabase 数据源（字段 P1 已备好）；② 后台风控处置写回 content_moderation 记录；③ OpenAPI/MCP 统一 ak 鉴权与计费；④ 邮件邀请接 Resend、发布改"物料导出+打卡"；⑤ A/B 变体接入 CSV/Webhook 投放回流 | P1 状态机与日志就绪 | 后台可在生产看到真实任务 Attempt/错误码/耗时；一笔真实订单在商家端与后台双向可查；ak 调用产生真实 credit 流水 |
| **P3：效果与成本治理**（第 15 周起） | 从"能生成"到"生成得划算、可评估" | ① 模型调用台账表（token/时长/成本按 action）+ 预算护栏（日成本上限）；② Prompt 模板版本化进入网关链路，A/B Prompt；③ 建 50-100 条评估集跑脚本质量回归；④ 真直连至少一个官方模型通道 + 健康探测驱动 fallback；⑤ 巨型页面拆分与最小单测门禁（CI：tsc+biome+计费单测） | P1/P2 数据积累 | 月报可输出单条视频真实算力成本；主通道故障演练自动切换并留痕；核心链路回归测试在 CI 阻断率>0 的破坏性变更可拦截 |

---

## 四、补充治理建议（速赢项清单）

1. **文档一致性**：README/商业计划书中的「SHA-256 缓存」「100ms 无缝降级」「XGBoost 预测」「口型毫秒对齐」「分片断点续传」等表述与代码不符，建议统一改为「Roadmap/实验性」措辞，避免评审与尽调质疑（零成本、高价值）。
2. **死代码清理**：`src/routes.tsx`、`src/pages/VideoCreatePage.tsx` 与 HomePage 的重复向导逻辑（1900 行）、`.dbg/`、`scratch/` 建议归档删除。
3. **配置与部署**：`vercel.json` 与双 vite config 并存，建议明确单一部署入口并在 README 写死版本；supabase `config.toml` 中函数 memory/timeout 显式声明。
4. **错误处理范式**：Edge Functions 的 `catch { 降级放行 }` 注释块共 10+ 处，统一替换为「logError + 明确错误码返回」，fail-open 仅留给白名单演示账号。
5. **积分事件**：以 `supabase.channel('credit_logs:...')` Realtime 替换 `window.dispatchEvent`，消除多标签页余额不一致。

---

## 五、结论

Shopro AI 的真实家底是：**一条「认证→商品→脚本→生成→扣费→支付」的可演示、可下单的主干链路 + 一套设计良好但尚未收口的 BaaS 数据层**；外围的监控、发布、协作、数据智能多数是精心包装的演示态，厂商后台是契约完整的纯前端原型。工程完成度约为主干的 70%、全平台的 35-40%。

按本路线图执行后优先级排布：**P0 两周内止血安全与计费（这是上线前置条件，不是可选项）；P1 用服务端任务状态机解决架构性缺陷；P2 让运营后台与开放能力从演示走向可运营；P3 做成本与效果的精细化治理**。每一步均可独立验收，且不需要推翻现有技术栈——Supabase 侧的表、RPC、Realtime、Cron 能力大多"字段先行"已备好，欠缺的只是执行引擎与治理纪律。
